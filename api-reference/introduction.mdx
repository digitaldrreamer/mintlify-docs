---
title: 'Introduction'
description: 'SD Proctor API Gateway — OpenAPI-powered reference'
---

<Note>
  This API reference is auto-sourced from the backend’s generated OpenAPI (swagger.json), ensuring it stays aligned with the deployed API Gateway.
</Note>

## Welcome

There are two ways to build API documentation: [OpenAPI](https://mintlify.com/docs/api-playground/openapi/setup) and [MDX components](https://mintlify.com/docs/api-playground/mdx/configuration). This site uses the generated OpenAPI specification from the API Gateway.

<Card
  title="SD Proctor API Endpoints"
  icon="leaf"
  href="/api-reference/openapi.json"
>
  View the OpenAPI specification file used by this site
</Card>

## Authentication

Most API endpoints require Bearer tokens. Use the Authorize button in the live playground to test authenticated calls.

```json
"security": [
  {
    "bearerAuth": []
  }
]
```
